describe("CallTotal", function () {

    it("should be able to set total amount", function () {
        let settingsTest = CalculateBill();

        // settingsTest.setCallTotal("call");
        // assert.equal(2.75, settingsTest.getCallTotal());

    });

    // it("total must show colour code for warning and danger level", function () {
    //     let settingsTest = CalculateBill();

    //     // settingsTest.setCallTotal("call");
    //     // assert.equal(2.75, settingsTest.getCallTotal());

    // });


});     